import React from 'react';
import ReactDOM from 'react-dom';

const App = () => (
  <div>
    <h1>Hello World!!! This is Nishitha and it's my frontend service!</h1>
  </div>
);

ReactDOM.render(<App />, document.getElementById('root'));
